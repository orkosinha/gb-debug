# gb-debug

Web-based debugger for [gb-emu](https://github.com/orkosinha/gb-emu).

Loads any `.gb` / `.gbc` ROM and exposes CPU, PPU, memory, disassembly, VRAM, APU, camera, and MBC7 accelerometer panels in the browser.

## Usage

```sh
mise run dev   # build + serve at http://localhost:3000
```
